---
- hosts: kube_control_plane
  gather_facts: false
  become: true
  tasks:
    - name: Ensure /storage is defined in fstab by UUID
      ansible.builtin.mount:
        src: "/dev/disk/by-uuid/{{ storage_blkid }}"
        path: /storage
        fstype: ext4
        opts: "defaults,noatime,nodiratime,commit=60"
        dump: 0
        passno: 2
        state: present
      when: storage_blkid is defined
